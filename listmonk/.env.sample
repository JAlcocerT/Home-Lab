# listmonk / docker-compose environment sample
# Copy this file to `.env` and adjust values as needed.

# --- Listmonk application ---
# Admin user that will be created automatically on first `docker compose up`.
# If you leave these empty, you will be prompted to create an admin via the UI.
LISTMONK_ADMIN_USER=admin
LISTMONK_ADMIN_PASSWORD=change-me-please

# Timezone used by the listmonk container
TZ=Etc/UTC

# --- Database (optional overrides) ---
# Your docker-compose.yml currently hardcodes these to "listmonk" via YAML anchors.
# Uncomment and adjust if you decide to switch to using env-variable substitution
# in docker-compose.yml in the future.
# POSTGRES_USER=listmonk
# POSTGRES_PASSWORD=listmonk
# POSTGRES_DB=listmonk

# Hostname used by the listmonk app container (for links, etc.)
# Not currently wired to docker-compose.yml, but you may want to keep a record here.
# LISTMONK_HOSTNAME=listmonk.example.com
