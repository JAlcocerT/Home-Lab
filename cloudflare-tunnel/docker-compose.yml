
services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command: tunnel --no-autoupdate run --token fill_your_token_here_please #CF as NS for your domain required
    networks:
      - tunnel
    restart: unless-stopped

networks:
  cloudflared_tunnel: